import { OtpScreen } from "@MR/screens";
import { SafeAreaView } from "react-native-safe-area-context";

export default () => {

    return (
        <SafeAreaView>
            <OtpScreen />
        </SafeAreaView>
    )
}