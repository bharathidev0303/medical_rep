import { OrderHistoryScreen } from "@MR/screens";
import { View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default () => {

    return (
        <SafeAreaView >
            <OrderHistoryScreen />
        </SafeAreaView>
    )
}